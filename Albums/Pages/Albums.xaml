<UserControl x:Class="Albums.Pages.Albums"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:ViewModel="clr-namespace:Albums.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="592.5">
    <UserControl.DataContext>
        <ViewModel:AlbumViewModel/>
    </UserControl.DataContext>
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0">
            <Label Width="100">Select Album</Label>
            <ComboBox DisplayMemberPath="Name" ItemsSource="{Binding AlbumCollection}" SelectedIndex="0" SelectedItem="{Binding SelectedAlbum}" Width="100">
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="1">
            <Label Width="100">Delete Album</Label>
            <Button Name="btnDeleteAlbum" Command="{Binding DeleteAlbum}" Width="100">Delete</Button>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="2">
            <Label Width="100">Add Photo</Label>
            <Button Name="btnOpenFile" Command="{Binding AddPhoto}" Width="100">Browse..</Button>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="3">
            <Label Width="100">AlbumName:</Label>
            <TextBox Text="{Binding AlbumName}" Width="100"/>
            <Button Width="100" Content="Create" Command="{Binding CreateAlbum}"/>
        </StackPanel>
        <WrapPanel Grid.Row="1" Grid.ColumnSpan="4">
            <StackPanel Orientation="Horizontal">
                <ListView ItemsSource="{Binding SelectedAlbum.Photos}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource Heading2}" Margin="0,0,0,8" />
                                <Image Source="{Binding Source}" Stretch="None"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </WrapPanel>
    </Grid>
</UserControl>
